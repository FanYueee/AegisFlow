apiVersion: 1

providers:
  - name: aegisflow
    folder: AegisFlow
    type: file
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
